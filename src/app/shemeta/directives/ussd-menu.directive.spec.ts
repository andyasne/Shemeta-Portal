import { USSDMenuDirective } from './ussd-menu.directive';

describe('Menu.DirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new USSDMenuDirective();
    // expect(directive).toBeTruthy();
  });
});
